@((DotNet.Highcharts.Highcharts)Model)

@*<table class="table table-bordered">
    <thead>
        <tr>
            <th></th>
            @{
                int count = ViewBag.Count;
                for (int i = 1; i <= count; i++)
                {
                    <th>@i</th>
                }
            }
            <th>Total</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Def.%</td>
            @{
                double sum_per = 0;
                for (int i = 0; i < count; i++)
                {
                    var x = ViewBag.DataPerc[i];
                    if(x != null) {sum_per += x;}
                    <td>@x.ToString("0.00")</td>
                }
                double avg = sum_per / (double)count;
            }
            <td>@avg.ToString("0.00")</td>
        </tr>
       <tr>
            <td>Def.Qty</td>
            @{
                var sum_def = 0;
                for (int i = 0; i < count; i++)
                {
                    var x = ViewBag.DataDef[i];
                    if (x != null) {sum_def += x;}
                    <td>@x.ToString("0,0")</td>
                }
            }
            <td>@sum_def.ToString("0,0")</td>
        </tr>
        <tr>
            <td>PD Qty</td>
            @{
                var sum_prod = 0;
                for (int i = 0; i < count; i++)
                {
                    var x = ViewBag.DataProd[i];
                    if (x != null) {sum_prod += x;}
                    <td>@x.ToString("0,0")</td>
                }
            }
            <td>@sum_prod.ToString("0,0")</td>
        </tr>
    </tbody>
</table>*@